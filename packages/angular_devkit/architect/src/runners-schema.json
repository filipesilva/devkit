{
  "$schema": "http://json-schema.org/schema",
  "id": "RunnersSchema",
  "title": "Runners schema for validating a list of runners.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Link to schema."
    },
    "runners": {
      "additionalProperties": {
        "$ref": "#/definitions/runner"
      }
    }
  },
  "definitions": {
    "runner": {
      "type": "object",
      "description": "Target options.",
      "properties": {
        "class": {
          "type": "string",
          "description": "The runner class module."
        },
        "schema": {
          "type": "string",
          "description": "Schema for runner option validation."
        },
        "description": {
          "type": "string",
          "description": "Runner description."
        }
      },
      "required": [
        "class",
        "schema",
        "description"
      ]
    }
  }
}
