{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsLibrary",
  "title": "Eject Options Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project to eject.",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": [
    "project"
  ]
}